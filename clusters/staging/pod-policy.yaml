apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: frontend
  namespace: default
spec:
  imageRepositoryRef:
    name: podinfo
  policy:
    alphabetical:
      order: asc
